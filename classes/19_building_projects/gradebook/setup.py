from setuptools import setup


setup(name='gradebook',
      version='1.0',
      description='A simple test of setup.py and unittest.',
      packages=['gradebook'],
      test_suite="test",
      zip_safe=False)
